# Runtime CLI uses only the Python stdlib; the packages listed here support builds and tests.
build
setuptools
wheel
pytest

# GUI runtime
PyQt6
PyQt5

# GUI metadata/render sources
PyMuPDF
pypdf
pikepdf

